- if logged_in?
  == #{current_user}
  |
  = link_to t("users.my_account"), account_path
  |

  = link_to t("global.logout"), logout_path
- else
  = link_to(t("global.login"), new_session_path)
  - unless SystemVariable.get :private_mode
    |
    = link_to(t("users.register"), new_account_path)