$("#results").removeClass("loading");
table = "<%= escape_javascript(render :partial => 'table') %>";
$("#results").html(table);
pagination = "<%= escape_javascript(render :partial => 'pagination') %>";
$(".pagination.clearfix").html(pagination);