%h1
  == Edit relationship description #{@relationship_description.title} for dataset description #{@dataset_description.title}
  = link_to 'Back', dataset_description_path(@dataset_description)
  
= render :partial => 'form'